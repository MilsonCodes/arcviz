import React from 'react';
import { Container } from '@material-ui/core';

const Stack = (props) => {

    return (
        <Container>
            Stack
        </Container>
    )
}

export default Stack;